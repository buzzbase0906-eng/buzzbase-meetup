# BUZZBASE MEETUP VOL.03 — 告知サイト

完全招待制イベント **BUZZBASE MEETUP VOL.03** の告知サイトです。
`index.html` 1ファイルで完結しており、GitHub Pages でそのまま配信しています。

公開URL: **https://buzzbase.net/**

独自ドメイン（お名前.com で取得）で配信しています。
以前の `https://buzzbase0906-eng.github.io/buzzbase-meetup/` は、
GitHub が自動で新URLへ転送するため、古いリンクを渡していても切れません。

---

## 🤝 このサイトを引き継ぐ人へ（Claude に読ませる前提の手引き）

複数人で編集します。**触る前にこの章を最後まで読んでください。**

### いちばん大事なルール

**このリポジトリの `index.html` が唯一の正しい版です。**

Claude 同士は作業状態を共有できません。あなたの Claude と、他の人の Claude は
別々の場所で動いていて、お互いの手元を見ることはできません。
**共有できるのは GitHub だけ**です。

そのため、**編集を始める前に必ず GitHub から最新版を取得してください。**
手元に残っている古いコピーから作業すると、その間に入った他の人の変更を
まるごと上書きして消してしまいます。

Claude に頼むときは、こう言ってください。

> 作業前に GitHub の最新の index.html を取得して、それを元に編集して。

### 同時に編集しないこと

このサイトは1つのファイルでできているため、2人が同じタイミングで編集すると、
**あとから保存したほうの内容だけが残り、もう片方の作業は消えます。**
（Git の差分マージが効きません）

作業前にチームへ「今からサイト触ります」と一声かけ、
終わったら「終わりました」と伝える運用にしてください。

### 編集の手順

1. GitHub でこのリポジトリを開く
2. `index.html` を開いて編集（またはローカルに落として編集し、アップロード）
3. コミットすると **2分ほどで公開サイトに反映**されます
4. 反映確認は URL の末尾に `?v=数字` を付ける（キャッシュ回避）

### ファイル構成

| ファイル | 内容 |
| --- | --- |
| `index.html` | サイト本体（HTML / CSS / JS / 画像をすべて内包・約700KB） |
| `event.mp4` | ティザー映像（6.6MB・別ファイル） |
| `robots.txt` | クローラーへの指示（いまは巡回を許可） |
| `sitemap.xml` | 検索エンジンに渡すページ一覧 |
| `ogp.jpg` | SNSでシェアしたときに出る画像（1200×630） |
| `guest-*.webp` | 出演ゲストの写真 |
| `CNAME` | 独自ドメイン（buzzbase.net）の指定。**消すとURLが元に戻ります** |

ページ遷移は `#/numbers` のようなハッシュルーティングです。
サーバー側の処理は一切ありません。静的HTMLだけで動いています。

---

## 中身を変えるときに触る場所

`index.html` の `<script>` の冒頭に、**設定用の定数**がまとまっています。
文言や日付を変えるだけなら、ここだけを直せば済みます。

| 定数名 | 内容 |
| --- | --- |
| `EVENT` | 開催日・時間・会場・定員 |
| `SITE` | サイト名・キャッチコピー・主催表記 |
| `LINKS` | 申込フォームURL・Instagram・連絡先メール・運営会社サイト（`company`） |
| `CREW` | 運営メンバー（`show:false` で非表示にできる） |
| `PARTNERS` | パートナー・協賛（`logo` と `url`） |
| `NEWS` | お知らせ |
| `PROGRAM` | 当日のタイムテーブル |
| `STATS` | NUMBERS ページの数字 |
| `FAQ` | よくある質問 |
| `SHOW` | 各セクションの表示ON/OFF（`past` / `voice` / `sponsors`） |
| `GUESTS` | 出演ゲスト（`show:false` で非表示、`sns:"tiktok"` や `url:` でリンク先を変更） |
| `GUEST_PHOTOS` | 出演ゲストの写真（Instagram ID がキー） |

### よくある編集の例

```js
// 開催日を変える
const EVENT = { date:"2026.09.25 FRI", ... }

// クルーを再表示する（show:false を消すだけ）
{show:false, code:"C-02", name:"SOTA INUI", ... }
   ↓
{code:"C-02", name:"SOTA INUI", ... }

// 申込フォームの送信先を変える
const LINKS = { entryForm:"https://...", ... }
```

---

## ⚠️ 壊さないでほしいもの

以下は理由があってそうなっています。**変える前に必ず相談してください。**

- **`CNAME` ファイル**
  中身は `buzzbase.net` の1行だけです。消すと独自ドメインが外れ、
  URL が `buzzbase0906-eng.github.io/buzzbase-meetup/` に戻ります。
- **`<head>` の検索まわり一式**（title / description / OGP / JSON-LD）
  検索結果とSNSシェアの見え方を決めています。消すと検索に出にくくなります。
  非公開に戻す手順は、このファイルのいちばん下に書いてあります。
- **動画の遅延読み込み**（`preload="none"` と `data-src`）
  6.6MB の動画を最初から読むと、表示が20秒以上遅くなります。
  `preload="auto"` に戻さないでください。
- **`.brw` クラスの付いた `<br>`**
  PC でだけ改行するための仕組みです。ただの `<br>` に戻すと、
  スマホで不自然な位置に改行が入ります。
- **装飾の `z-index`**（`section:not(.hero) .floaters`）
  背景の飾りが本文の上に被らないようにしています。
- **参考サイトからの素材コピー**
  画像・ロゴ・音源・イラストを他所のサイトからコピーしないでください。
  自前の素材か、商用利用が許可されたものだけを使います。

---

## 掲載内容の状況

| 項目 | 状況 |
| --- | --- |
| クルー | 半谷一太のみ表示。他5名は `show:false` で保留中 |
| 出演ゲスト | 81名を掲載。残り17名は `show:false`（経営者9名＋掲載NG8名）。**NG の方を表示に戻すときは必ず運営へ確認** |
| 過去開催 / 参加者の声 / 協賛 | `SHOW` で非表示中（実データが揃うまで） |
| パートナーのロゴ | にいみ農園のみ。STREET PV と TOPICO は文字表記 |

---

## Claude に頼むときの伝え方

このリポジトリにアクセスできる状態で、こう伝えてください。

> buzzbase0906-eng/buzzbase-meetup の README を読んで、index.html を編集して。
> 変更したら GitHub にコミットまでして。

README を読ませることで、上の「壊さないでほしいもの」も引き継がれます。

---

## 検索エンジンへの公開について

**このサイトは検索結果に出る設定になっています。**

- `index.html` の `<head>` に、タイトル・説明文・OGP・イベントの構造化データ（JSON-LD）を入れています
- `robots.txt` は `Allow: /`、`sitemap.xml` も置いています
- シェア用の画像は `ogp.jpg`（1200×630）

**非公開に戻したいとき**は2か所を直します。

1. `index.html` の `<meta name="robots" content="index, follow, ...">` を
   `<meta name="robots" content="noindex, nofollow">` に
2. `robots.txt` の `Allow: /` を `Disallow: /` に

会場名は載せない方針です。構造化データにも「東京・渋谷」までしか書いていません。

**リポジトリは公開されています。パスワードや応募者の個人情報は絶対に書かないでください。**
